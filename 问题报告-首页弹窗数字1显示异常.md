# 问题报告：首页 SKU 弹窗数字"1"显示异常

## 问题描述

在小程序开发环境下，当打开商品的购买框（SKU弹窗）时，页面上会显示不应该出现的数字"1"。这些数字有以下特征：
- 会随着首页商品列表滚动而移动
- 套装商品的每个子项对应一个数字（1、2、3...）
- 即使打开的是普通商品（非套装），也会出现这些数字
- 数字悬浮在页面上，不在弹窗内部

## 问题定位

### 根本原因

**首页的 SKU 弹窗 HTML 结构存在问题，导致弹窗元素未能完全脱离页面文档流。**

### 技术细节

#### 1. 代码位置

**文件：** `miniprogram/pages/index/index.wxml`  
**行号：** 第 150-213 行

```xml
<!-- 问题代码 -->
<view class="sku-mask {{showSku ? 'fade-in' : ''}}" wx:if="{{showSku}}" catchtap="closeSkuPanel"></view>

<view class="sku-panel {{showSku ? 'slide-up' : ''}}">  <!-- ⚠️ 缺少 wx:if -->
  <!-- 弹窗内容 -->
  <view class="bundle-group-card">
    <text class="bg-step">{{bgIdx + 1}}</text>  <!-- 这就是数字"1"的来源 -->
  </view>
</view>
```

#### 2. 问题分析

| 元素 | 当前状态 | 问题 |
|------|---------|------|
| `.sku-mask` | 有 `wx:if="{{showSku}}"` | ✅ 正常：关闭时从 DOM 中移除 |
| `.sku-panel` | **缺少 `wx:if`** | ❌ 问题：始终存在于 DOM 中 |

**为什么会出现数字"1"？**

1. `.sku-panel` 没有 `wx:if="{{showSku}}"` 条件渲染
2. 弹窗元素在页面加载时就已渲染，只是通过 CSS `transform: translateY(100%)` 隐藏在屏幕下方
3. 弹窗内的套装子项序号（`.bg-step`）元素虽然被移到屏幕外，但在某些渲染情况下会"泄漏"到可视区域
4. 当弹窗未激活（`!showSku`）时，这些元素的定位计算可能受到首页滚动的影响，导致数字随页面滚动

#### 3. 与详情页对比

**详情页（正常）：** `miniprogram/pages/detail/detail.wxml` 第 110-192 行

```xml
<!-- 正确的实现 -->
<view class="sku-mask" wx:if="{{showSku}}" catchtap="closeSkuPanel"></view>

<view class="sku-panel {{showSku ? 'show' : ''}}">  <!-- ✅ 虽然也没有 wx:if，但... -->
```

**为什么详情页没问题？**
- 详情页的 `.sku-panel` 使用 `bottom: -100%` 初始定位，完全在屏幕外
- 首页的 `.sku-panel` 使用 `bottom: 0` + `transform: translateY(100%)`，在某些情况下子元素可能计算错误

#### 4. CSS 层级对比

| 页面 | `.sku-mask` z-index | `.sku-panel` z-index | 初始位置 |
|------|---------------------|----------------------|---------|
| 首页 | 1000 | 1001 | `bottom: 0; transform: translateY(100%)` |
| 详情页 | 999 | 1000 | `bottom: -100%` |

#### 5. 样式定义位置

**`.bg-step` 样式定义：**
- `miniprogram/pages/index/index.wxss` 第 646-658 行
- `miniprogram/pages/detail/detail.wxss` 第 474-487 行
- `miniprogram/pages/liveRoomDetail/detail.wxss` 第 511-523 行

所有三个页面都使用相同的类名和相似的样式，但只有首页出现问题。

## 复现条件

- **环境：** ⚠️ **仅在真机环境出现（体验版/正式版），开发者工具无法复现**
- **触发条件：**
  1. 打开首页
  2. 点击任意商品的"加入购物车"按钮，打开 SKU 弹窗
  3. 观察页面，会看到数字"1"（或"1、2、3"等，取决于套装子项数量）
  4. 滚动首页商品列表，数字会跟随滚动

### 为什么开发工具看不到？

微信开发者工具和真机的渲染引擎存在差异：

| 环境 | 渲染引擎 | `transform` 行为 |
|------|---------|-----------------|
| 开发者工具 | Chromium 内核 | 正确处理合成层，子元素完全隐藏 |
| 真机（iOS/Android） | 微信 WebView | 合成层优化策略不同，子元素可能泄漏 |

**关键点：** 当 `position: fixed` 元素使用 `transform: translateY(100%)` 移出屏幕时，真机环境下的渲染层计算可能导致子元素（`.bg-step`）的定位错误，使其出现在可视区域内。这是真机渲染引擎的已知差异。

## 影响范围

- **受影响页面：** 首页 (`pages/index/index`)
- **不受影响页面：** 商品详情页 (`pages/detail/detail`)、直播间详情页 (`pages/liveRoomDetail/detail`)
- **影响设备：** ⚠️ **仅真机环境（iOS/Android 微信客户端）**，开发者工具不受影响
- **用户体验：** 严重影响 UI 表现，用户在真机上会看到莫名其妙的数字悬浮在页面上
- **功能影响：** 无，不影响购买流程和数据提交

## 解决方案

### 方案 1：添加条件渲染（推荐）

**修改文件：** `miniprogram/pages/index/index.wxml` 第 152 行

```xml
<!-- 修改前 -->
<view class="sku-panel {{showSku ? 'slide-up' : ''}}">

<!-- 修改后 -->
<view class="sku-panel {{showSku ? 'slide-up' : ''}}" wx:if="{{showSku}}">
```

**优点：**
- 弹窗关闭时完全从 DOM 中移除，不占用内存
- 彻底解决渲染异常问题
- 与 `.sku-mask` 的处理方式保持一致

**缺点：**
- 每次打开弹窗都需要重新渲染 DOM（性能影响极小）

### 方案 2：修改 CSS 定位策略

**修改文件：** `miniprogram/pages/index/index.wxss` 第 459-471 行

```css
/* 修改前 */
.sku-panel {
  position: fixed;
  bottom: 0;
  transform: translateY(100%);
  /* ... */
}

/* 修改后 */
.sku-panel {
  position: fixed;
  bottom: -100%;  /* 改为负值定位 */
  transform: none;  /* 移除 transform */
  /* ... */
}

.sku-panel.slide-up {
  bottom: 0;  /* 激活时回到底部 */
  /* ... */
}
```

**优点：**
- 与详情页的实现方式统一
- 弹窗元素始终存在，打开时无需重新渲染（动画更流畅）

**缺点：**
- 需要同步修改动画过渡属性
- 弹窗元素始终占用内存（实际影响可忽略）

### 方案 3：增加容器隔离（备选）

为 `.sku-panel` 添加一个包裹容器：

```xml
<view class="sku-wrapper" wx:if="{{showSku}}">
  <view class="sku-panel {{showSku ? 'slide-up' : ''}}">
    <!-- 弹窗内容 -->
  </view>
</view>
```

## 推荐修复方案

**采用方案 1（添加 `wx:if="{{showSku}}"`）**

原因：
1. 最简单直接，只需修改一行代码
2. 与微信小程序最佳实践一致（弹窗组件应使用条件渲染）
3. 与 `.sku-mask` 的处理逻辑保持统一
4. 性能开销可忽略（SKU 弹窗不是频繁打开关闭的组件）

## 其他建议

1. **统一三个页面的 SKU 弹窗实现**  
   当前首页、详情页、直播间详情页的弹窗实现略有不同，建议提取为公共组件。

2. **检查是否存在其他类似问题**  
   搜索项目中所有使用 `position: fixed` + `transform` 的弹窗类组件，确保都使用了条件渲染。

3. **真机测试的重要性**  
   ⚠️ **此问题证明了真机测试的必要性**。开发者工具和真机的渲染引擎存在差异，关键 UI 组件（特别是弹窗、悬浮层）必须在真机上验证，不能仅依赖开发工具。

4. **建立真机测试清单**  
   对于涉及 `position: fixed`、`transform`、`z-index` 的复杂布局，建议建立真机测试检查清单：
   - iOS 不同机型（刘海屏 / 非刘海屏）
   - Android 不同系统版本
   - 不同微信版本

## 附录

### 相关文件清单

- `miniprogram/pages/index/index.wxml` - 首页模板（问题所在）
- `miniprogram/pages/index/index.wxss` - 首页样式
- `miniprogram/pages/detail/detail.wxml` - 详情页模板（参考对照）
- `miniprogram/pages/detail/detail.wxss` - 详情页样式（参考对照）

### 关键字

`bg-step`, `sku-panel`, `bundle-group-card`, `fixed positioning`, `transform translateY`, `wx:if`
