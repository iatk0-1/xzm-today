# xzm-today 迁移完成总结

## 一、已完成的工作

### 1.1 创建的工具类文件

```
miniprogram/utils/
├── config.js       # 配置文件（API 地址、小程序 AppID 等）
├── api.js          # API 请求封装（get/post/put/patch/delete/uploadFile）
└── auth.js         # 认证管理（login/checkLogin/getUserInfo/isAdmin 等）
```

### 1.2 修改的文件

| 文件 | 修改内容 |
|------|---------|
| `miniprogram/app.js` | 移除 `wx.cloud.init()`，改用本地认证检查 |
| `pages/index/index.js` | 商品列表改用 `GET /api/v1/products/search` |
| `pages/detail/detail.js` | 商品详情改用 `GET /api/v1/products/{id}` |
| `pages/cart/cart.js` | 保持本地存储，无需修改 |
| `pages/checkout/checkout.js` | 订单创建和微信支付改用后端 API |
| `pages/orderList/orderList.js` | 订单列表改用 `GET /api/v1/orders` |
| `pages/orderDetail/orderDetail.js` | 订单详情改用 `GET /api/v1/orders/{id}` |
| `pages/user/user.js` | 管理员检查改用 `auth.isAdmin()` |
| `pages/market/market.js` | 心愿列表和点赞改用后端 API |
| `pages/publishWish/publishWish.js` | 图片上传和心愿创建改用后端 API |
| `pages/search/search.js` | 搜索改用 `GET /api/v1/products/search` |
| `pages/admin/admin.js` | 商品发布改用后端文件上传和创建 API |
| `pages/adminProduct/adminProduct.js` | 商品上下架和删除改用后端 API |
| `pages/adminOrder/adminOrder.js` | 订单管理和发货改用后端 API |

---

## 二、后端接口使用情况

### 2.1 已使用的后端接口

| 功能 | 接口 | 说明 |
|------|------|------|
| 商品列表 | `GET /api/v1/products/search` | 支持 keyword 搜索、tag 筛选 |
| 商品详情 | `GET /api/v1/products/{id}` | 返回 product + skus |
| 关联商品 | `GET /api/v1/products/related` | 根据 ids 获取关联商品 |
| 创建商品 | `POST /api/v1/products` | 需要 ADMIN 角色 |
| 更新商品状态 | `PATCH /api/v1/products/{id}/status` | on/off |
| 删除商品 | `DELETE /api/v1/products/{id}` | 需要 ADMIN 角色 |
| 订单列表 | `GET /api/v1/orders` | 支持 status 筛选 |
| 订单详情 | `GET /api/v1/orders/{id}` | 返回订单详情 |
| 创建订单 | `POST /api/v1/orders` | 传入 items 数组 |
| 微信支付 | `POST /api/v1/orders/{id}/pay/wechat` | 返回支付参数 |
| 确认收货 | `POST /api/v1/orders/{id}/receive` | 更新订单状态 |
| 管理端订单 | `GET /api/v1/admin/orders` | 需要 ADMIN 角色 |
| 心愿列表 | `GET /api/v1/wishes` | 返回所有心愿 |
| 创建心愿 | `POST /api/v1/wishes` | 兼容 image，并支持 images、title、content、expectedPrice |
| 按图片批量创建心愿 | `POST /api/v1/wishes/batch` | 每张图片创建一条心愿，事务内全成或全败 |
| 点赞心愿 | `POST /api/v1/wishes/{id}/like` | 点赞 |
| 取消点赞 | `POST /api/v1/wishes/{id}/unlike` | 取消点赞 |
| 文件上传 | `POST /api/v1/files/upload-wish` | 上传心愿图片 |
| 管理端上传 | `POST /api/v1/admin/files/upload` | 上传商品图片 |
| 创建发货单 | `POST /api/v1/orders/{id}/shipments` | 需要 ADMIN 角色 |

### 2.2 后端状态映射

前端使用的状态与后端状态对应关系：

| 前端中文 | 后端英文 |
|---------|---------|
| 待付款 | `PENDING_PAYMENT` |
| 待发货 | `PAID` |
| 已发货 | `SHIPPED` |
| 已完成 | `COMPLETED` |
| 退款/售后 | `AFTER_SALE` |
| 已关闭 | `CLOSED` |
| 上架 | `on` |
| 下架 | `off` |

---

## 三、仍需注意的事项

### 3.1 后端需要启动

小程序目前配置的后端地址是 `http://localhost:8080`，需要确保：
1. 后端服务已启动
2. 端口 8080 可访问
3. 数据库连接正常

### 3.2 微信开发者工具设置

在微信开发者工具中需要：
1. 勾选 "不校验合法域名、web 视图（业务域名）、TLS 版本以及 HTTPS 证书"
2. 或者在 `project.config.json` 中配置 `setting.checkSite`: false

### 3.3 管理员账号

当前配置使用 mock OpenID 登录：
```javascript
// miniprogram/utils/config.js
ADMIN_OPENID: 'owehQ3RIu5bhzKM0HtEX7gxqU2Jw'
```

登录时会自动使用这个 OpenID 进行 mock 登录。

### 3.4 购物车保持本地存储

购物车功能目前仍使用 `wx.setStorageSync` 本地存储，没有与后端同步。如需云端同步购物车，可以使用：
- `GET /api/v1/cart` - 获取购物车
- `POST /api/v1/cart/items` - 添加商品
- `PUT /api/v1/cart/items/{itemId}` - 更新商品
- `DELETE /api/v1/cart/items/{itemId}` - 删除商品

---

## 四、测试步骤

### 4.1 启动后端
```bash
cd D:\ideaProjects\xzm-backend
# 确保 .env.local 或 .env.dev 配置正确
mvn spring-boot:run
```

### 4.2 打开小程序
1. 打开微信开发者工具
2. 导入项目 `D:\ideaProjects\xzm-today`
3. 勾选 "不校验合法域名"
4. 编译运行

### 4.3 功能测试清单

- [ ] 首页商品列表加载
- [ ] 商品详情页加载
- [ ] SKU 选择和价格计算
- [ ] 加入购物车
- [ ] 购物车全选/单选
- [ ] 结算页地址选择
- [ ] 订单创建
- [ ] 微信支付（需要配置真实商户号）
- [ ] 订单列表查询
- [ ] 订单详情查询
- [ ] 确认收货
- [ ] 心愿列表
- [ ] 心愿点赞
- [ ] 发布心愿（图片上传）
- [ ] 商品搜索
- [ ] 管理员商品发布
- [ ] 商品上下架
- [ ] 订单发货管理

---

## 五、可能遇到的问题

### 5.1 网络请求失败
**错误**: "无法连接服务器，请检查后端是否启动"
**解决**: 确保后端服务在 `http://localhost:8080` 运行

### 5.2 登录已过期
**错误**: "登录已过期"
**解决**: 清除本地存储，重新进入小程序会自动登录

### 5.3 图片上传失败
**错误**: 上传文件时返回错误
**解决**: 检查后端 MinIO 或其他存储服务是否正常运行

### 5.4 字段不匹配
某些后端返回的字段可能与前端期望的不一致：
- 后端 `id` (Long) vs 前端 `_id` (String)
- 后端 `coverUrl` vs 前端 `image`
- 后端 `name` vs 前端 `title`

已在代码中做了兼容性处理，同时保留两种字段名。

---

## 六、配置文件说明

### 6.1 config.js
```javascript
module.exports = {
  API_BASE_URL: 'http://localhost:8080/api/v1',
  MINIAPP_APPID: 'wx7692230ac8ecdaa8',
  TOKEN_KEY: 'accessToken',
  REFRESH_TOKEN_KEY: 'refreshToken',
  USER_INFO_KEY: 'userInfo',
  ADMIN_OPENID: 'owehQ3RIu5bhzKM0HtEX7gxqU2Jw'
};
```

### 6.2 修改后端地址
如需连接远程后端，修改 `config.js` 中的 `API_BASE_URL`：
```javascript
API_BASE_URL: 'https://your-domain.com/api/v1'
```

---

## 七、后续优化建议

1. **购物车云端同步**: 实现多设备购物车同步
2. **用户地址管理**: 对接后端地址管理接口
3. **物流查询**: 对接后端物流轨迹接口
4. **错误处理优化**: 统一的错误处理和重试机制
5. **Loading 优化**: 使用统一的 loading 管理器
6. **Token 刷新**: 实现无感知的 Token 刷新机制

---

## 八、迁移完成日期

2026-03-22
