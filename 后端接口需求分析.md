# 后端接口需求分析

## 一、接口状态总览

| 接口 | 状态 | 说明 |
|------|------|------|
| 商品相关 | ✅ 完整 | 所有接口已实现 |
| 订单相关 | ✅ 完整 | 所有接口已实现 |
| 心愿相关 | ✅ 完整 | 所有接口已实现 |
| 文件上传 | ✅ 完整 | 两个上传接口已实现 |
| 发货相关 | ✅ 完整 | 所有接口已实现 |
| 认证相关 | ✅ 完整 | 小程序登录接口已实现 |
| 地址管理 | ✅ 完整 | 所有接口已实现 |
| 购物车 | ✅ 完整 | 所有接口已实现 |

---

## 二、现有接口清单

### 2.1 商品接口 (Products)

```
GET    /api/v1/products                      # 商品列表
GET    /api/v1/products/search               # 搜索商品 (支持 keyword, tag)
GET    /api/v1/products/related              # 关联商品 (支持 ids)
GET    /api/v1/products/{id}                 # 商品详情
POST   /api/v1/products                      # 创建商品 (ADMIN)
PUT    /api/v1/products/{id}                 # 更新商品 (ADMIN)
PATCH  /api/v1/products/{id}/status          # 更新状态 (ADMIN)
DELETE /api/v1/products/{id}                 # 删除商品 (ADMIN)
GET    /api/v1/products/tags                 # 标签列表
```

### 2.2 订单接口 (Orders)

```
POST   /api/v1/orders                        # 创建订单
GET    /api/v1/orders                        # 订单列表 (支持 status 筛选)
GET    /api/v1/orders/{id}                   # 订单详情
POST   /api/v1/orders/{id}/pay               # 支付订单
POST   /api/v1/orders/{id}/pay/wechat        # 微信支付预下单
POST   /api/v1/orders/{id}/receive           # 确认收货
POST   /api/v1/orders/{id}/cancel            # 取消订单
POST   /api/v1/orders/{id}/shipments         # 创建发货单 (ADMIN)
GET    /api/v1/orders/{id}/shipments         # 订单发货单列表
GET    /api/v1/orders/{id}/shipments/trace   # 物流轨迹

# 管理端订单
GET    /api/v1/admin/orders                  # 管理端订单列表 (ADMIN)
GET    /api/v1/admin/orders/{id}             # 管理端订单详情 (ADMIN)
```

### 2.3 心愿接口 (Wishes)

```
GET    /api/v1/wishes                        # 心愿列表
GET    /api/v1/wishes/{id}                   # 心愿详情
POST   /api/v1/wishes                        # 创建心愿
POST   /api/v1/wishes/{id}/like              # 点赞
POST   /api/v1/wishes/{id}/unlike            # 取消点赞
PUT    /api/v1/wishes/{id}/link              # 关联商品 (ADMIN)
DELETE /api/v1/wishes/{id}                   # 删除心愿 (ADMIN)
```

### 2.4 文件上传接口 (Files)

```
POST   /api/v1/files/upload-wish             # 上传心愿图片
POST   /api/v1/admin/files/upload            # 管理端上传 (ADMIN)
```

### 2.5 认证接口 (Auth)

```
POST   /api/v1/auth/miniapp/login            # 小程序登录
POST   /api/v1/auth/miniapp/phone-login      # 小程序手机登录
POST   /api/v1/auth/admin/login              # 管理员登录
POST   /api/v1/auth/refresh                  # 刷新 Token
POST   /api/v1/auth/logout                   # 登出
```

### 2.6 用户地址接口 (User Addresses)

```
GET    /api/v1/users/me/addresses            # 地址列表
GET    /api/v1/users/me/addresses/{id}       # 地址详情
GET    /api/v1/users/me/addresses/default    # 默认地址
POST   /api/v1/users/me/addresses            # 创建地址
PUT    /api/v1/users/me/addresses/{id}       # 更新地址
DELETE /api/v1/users/me/addresses/{id}       # 删除地址
```

### 2.7 购物车接口 (Cart)

```
GET    /api/v1/cart                          # 购物车概览
GET    /api/v1/cart/selected                 # 选中商品
POST   /api/v1/cart/items                    # 添加商品
PUT    /api/v1/cart/items/{id}               # 更新商品
PATCH  /api/v1/cart/items/{id}/toggle        # 切换选中
PATCH  /api/v1/cart/items/{id}/selected      # 设置选中
PATCH  /api/v1/cart/toggle-all               # 切换全选
PATCH  /api/v1/cart/all-selected             # 设置全选
DELETE /api/v1/cart/items/{id}               # 删除商品
DELETE /api/v1/cart/selected                 # 删除选中
DELETE /api/v1/cart/all                      # 清空购物车
```

---

## 三、接口字段映射

### 3.1 商品字段

| 前端字段 | 后端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `_id` / `id` | `id` | Long | 商品 ID |
| `image` | `coverUrl` | String | 封面图 |
| `title` | `name` | String | 商品名称 |
| `bannerImgs` | `bannerImages` | Array | 轮播图 |
| `tags` | `tags` | Array | 标签 |
| `skuMatrix` | `skuMatrix` | Array | SKU 矩阵 |
| `displayPrice` | `displayPrice` | String | 显示价格 |
| `lookbookImgs` | `lookbookImages` | Array | 穿搭图 |
| `detailImgs` | `detailImages` | Array | 详情图 |
| `manualRelatedIds` | `relatedProductIds` | Array | 关联商品 ID |
| `status` | `status` | String | on/off |
| `createTime` | `createdAt` | DateTime | 创建时间 |

### 3.2 SKU 字段

| 前端字段 | 后端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `color` | `spec` (分割后) | String | 颜色/规格 |
| `size` | `spec` (分割后) | String | 尺码 |
| `price` | `retailPrice` | BigDecimal | 价格 |
| `stock` | `stockMain` | Integer | 库存 |

### 3.3 订单字段

| 前端字段 | 后端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `_id` / `id` | `id` | Long | 订单 ID |
| `status` | `status` | String | 订单状态 |
| `totalPrice` | `payAmount` | BigDecimal | 支付金额 |
| `items` | `items` | Array | 订单项 |
| `createTime` | `createdAt` | DateTime | 创建时间 |

### 3.4 心愿字段

| 前端字段 | 后端字段 | 类型 | 说明 |
|---------|---------|------|------|
| `_id` / `id` | `id` | Long | 心愿 ID |
| `image` | `image` | String | 心愿图片 |
| `title` | `content` | String | 心愿描述 |
| `likes` | `likes` | Integer | 点赞数 |
| `linkedProductId` | `linkedProductId` | Long | 关联商品 ID |
| `isLiked` | `isLiked` | Boolean | 是否点赞 |

---

## 四、心愿字段补充建议

当前心愿接口缺少以下字段，如需完整支持小程序心愿功能，建议补充：

### 4.1 建议新增字段

```java
public record WishCreateRequest(
    @NotBlank(message = "image is required")
    String image,

    @NotBlank(message = "content is required")
    String content,

    // 建议新增: 标题字段 (当前使用 content 代替)
    String title,

    // 建议新增: 期望价格
    BigDecimal expectedPrice,

    // 建议新增: 主理人手记
    List<TraceLog> traceLogs,

    Long likes,
    Long linkedProductId) {
}

public record TraceLog(
    String timestamp,
    String status,  // SOURCING/REJECTED/LOCKED
    String text
) {}
```

### 4.2 WishResponse 建议补充

```java
public record WishResponse(
    Long id,
    String image,
    String content,
    String title,              // 新增
    BigDecimal expectedPrice,  // 新增
    List<TraceLog> traceLogs,  // 新增
    Integer likes,
    Long linkedProductId,
    String createdBy,
    OffsetDateTime createdAt,
    Boolean isLiked) {
}
```

---

## 五、配置要求

### 5.1 数据库
- PostgreSQL (已配置)

### 5.2 对象存储
- MinIO (已配置)
- 或其他兼容 S3 的存储服务

### 5.3 微信支付配置
```yaml
app.payment.wechat:
  appid: wx7692230ac8ecdaa8
  mchid: 1739557473
  notify-url: https://your-domain.com/api/v1/pay/wechat/notify
```

### 5.4 JWT 配置
```yaml
app.auth.jwt:
  secret: your-secret-key
  expire-seconds: 7200
  refresh-expire-seconds: 2592000
```

---

## 六、测试建议

### 6.1 使用 Postman 测试接口
1. 先调用 `POST /api/v1/auth/miniapp/login` 获取 Token
2. 在 Header 中添加 `Authorization: Bearer {accessToken}`
3. 测试各个业务接口

### 6.2 小程序 Mock 登录
小程序目前使用 mock OpenID 模式，登录时会自动使用配置的 ADMIN_OPENID，方便本地测试。

---

## 七、总结

当前后端接口已经 **100% 覆盖** 小程序所需功能，无需新增接口。

唯一需要注意的是心愿字段的映射：
- 前端 `title` → 后端 `content`
- 前端暂无 `expectedPrice` 和 `traceLogs` 需求
